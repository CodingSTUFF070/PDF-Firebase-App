<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PdfFirebaseApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/secondary_color</item>
    </style>

    <style name="Theme.PdfFirebaseApp" parent="Base.Theme.PdfFirebaseApp" />
</resources>